<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.routineapp">

    <uses-feature
        android:name="android:hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".classesbyteacher"></activity>
        <activity
            android:name=".wholeweekroutine"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".takeattendance" />
        <activity android:name=".attendance" />
        <activity
            android:name=".setting"/>
        <activity android:name=".profile" />
        <activity
            android:name=".subjectchat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".edityearbranch" />
        <activity
            android:name=".login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>